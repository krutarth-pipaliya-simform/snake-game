{
  "name": "snake-game-monorepo",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.0",
    "lint-staged": "^15.4.0"
  },
  "lint-staged": {
    "FrontEnd/**/*.{ts,tsx}": [
      "oxlint --fix"
    ],
    "BackEnd/**/*.ts": [
      "eslint --fix",
      "prettier --write"
    ]
  }
}
