{
  "name": "snake-game-monorepo",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.0",
    "lint-staged": "^15.4.0"
  },
  "lint-staged": {
    "FrontEnd/**/*.{ts,tsx}": [
      "npx --no-install oxlint --fix"
    ],
    "BackEnd/**/*.ts": [
      "npx --no-install eslint --fix",
      "npx --no-install prettier --write"
    ]
  }
}
